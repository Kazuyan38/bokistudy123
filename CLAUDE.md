# BokiStudy — Claude運用ルール

日商簿記2級合格を目指すユーザーの学習システム。学習はアプリ（GitHub Pages）で完結し、**学習進行の管理はアプリの学習エンジンが行う**。Claudeは進行管理をしない。

正本は [design/DESIGN.md](design/DESIGN.md)。スキーマ・数値基準・運用の詳細は必ずそちらに従う。

## Claudeの役割（この3つのみ）

1. **アプリ開発・改善** — vanilla HTML/CSS/JSのみ。ビルドツール・外部JSライブラリ・npm禁止（Google Fontsのみ可）。`docs/` がそのままGitHub Pagesで動くこと
2. **コンテンツ作成** — レッスン・問題・模試のJSONを DESIGN.md §4 のスキーマに厳密準拠で作成
3. **質問対応** — ユーザーの簿記の質問に回答（下記「質問対応モード」）

## コンテンツ作成ルール

- **著作権**: 市販教材・日商過去問・予想問題集の転載/改変転載は禁止。完全オリジナル作問。例示企業は架空名（ハルカ商事・ミナト製作所・アオイ物産 等）
- 文体: です・ます調。1セクション本文300〜600字。金額は暗算しやすい数値
- ID規則: 単元 `s{n}-u{nn}`／問題 `q-{unitId}-{連番3桁}`／模試 `mock-{級}{連番2桁}`／論点 `t-{slug}`
- 単元あたり: レッスン3〜6セクション（各セクションにチェックポイント2〜4問）、ドリル15〜40問（`srs:true` コアカード8〜15問）、全問に論点タグ・解説必須
- 作問時は DESIGN.md §7 パターン網羅表と照合し、必要なら表に行を追加
- 新しい勘定科目は `docs/data/accounts.json` に登録（introducedIn を正しく設定）
- 追加後は必ず: `docs/data/curriculum.json` の該当単元を `available` に更新 → `node` があれば JSON構文チェック → `docs/dev/validate.html` での検証をユーザーに案内
- コミットメッセージ例: `s1-u04: 手形・電子記録債権のレッスン+問題28問追加`

## 質問対応モード

- 簿記の内容の質問には、該当単元のレッスンJSON（`docs/data/lessons/`）を読み、**その単元の教え方・用語に合わせて**回答する
- ユーザーが「質問用サマリー」（アプリの設定画面から生成）を貼ったら、弱点に即した説明・追加ドリル作成を提案する
- 教材に不足・誤りがあれば、レッスンへの追記・修正を提案する（勝手に方針変更しない）

## コネクタ運用フロー

- **サマリー貼付時**: Notion「BokiStudy 進捗ハブ」の単元進捗DB・間違いノートDBを更新 → 弱点対応 → 必要ならGoogle Calendarの予定調整
- **エクスポートJSON貼付時**: Google Drive「BokiStudy バックアップ」フォルダに日付名で保存
- **週1回**: Gmailで進捗レポート送信（学習量・正答率・弱点・翌週の重点）
- **単元合格の報告時**: Gammaで復習スライドデッキ、必要に応じCanvaでチートシート作成
- **Stage修了時**: SlidesGPTで総まとめ講義スライド＋Gmailで節目レポート
- **Slack**: 学習リマインダーDMを週分まとめて予約投稿（切れていたら補充）

## 注意事項

- リポジトリはpublic。個人情報（氏名・受験番号等）をリポジトリに書かない。学習データはlocalStorageのみ（リポジトリに置かない）
- OneDrive配下のため、作業はコミットを小まめに
- アプリURL: https://kazuyan38.github.io/bokistudy123/
